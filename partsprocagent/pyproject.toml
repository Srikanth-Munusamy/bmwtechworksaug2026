[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "partsprocagent-train-deploy"
version = "0.1.0"
description = "Train and deploy a scikit-learn model with parts processing agent"

requires-python = ">=3.10"
dependencies = [
    "openai",
    "streamlit",
    "boto3",
    "pandas",
    "httpx",
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "psycopg[binary]",
    "python-dotenv"
 
]

[project.optional-dependencies]
dev = ["pytest>=8,<9", "ruff>=0.9,<1"]

[tool.hatch.build.targets.wheel]
packages = ["src/parts_agent"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
