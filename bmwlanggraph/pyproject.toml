[project]

name = "inventory-langgraph"

version = "0.1.0"

requires-python = ">=3.13"

dependencies = [
    "langgraph",
    "langchain",
    "langchain-openai",
    "fastapi",
    "uvicorn",
    "streamlit",
    "requests",
    "python-dotenv"
]